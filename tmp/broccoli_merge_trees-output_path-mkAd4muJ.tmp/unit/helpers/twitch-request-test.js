
import { twitchRequest } from 'twitch-api/helpers/twitch-request';
import { module, test } from 'qunit';

module('Unit | Helper | twitch request');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = twitchRequest([42]);
  assert.ok(result);
});

